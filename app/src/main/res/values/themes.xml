<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LockIn" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customizations -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@color/transparent</item> <!-- Barra de status transparente -->
        <item name="android:navigationBarColor">@color/transparent</item> <!-- Barra de navegação transparente -->
        <item name="android:windowTranslucentStatus">true</item> <!-- Barra de status translúcida -->
        <item name="android:windowTranslucentNavigation">true</item> <!-- Barra de navegação translúcida -->
        <item name="android:fontFamily">@font/inter</item>
    </style>
</resources>
